# github-trustloop-demo

This folder contains intentionally imperfect utility functions for evaluating AI code-trust pipelines such as TrustLoop.

The code is designed to look realistic and readable while still containing weak assumptions, incomplete hardening, and edge cases that a review or adversarial test generator should uncover.

Files are small on purpose so they can be inspected quickly in a GitHub-based evaluation run.
